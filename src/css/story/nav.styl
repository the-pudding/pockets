nav
  overflow hidden
  width $column-width
  margin 0 auto
  position sticky
  &:after
    position absolute
    bottom 0
    display block
    content ''
    width 100%
    height 100%
    z-index 100
    pointer-events none
    right -80%
    background linear-gradient(to right, rgba($bg-denim,0) 0%,rgba($bg-denim,1) 25%,rgba($bg-denim,1) 100%)
    +mq('bp-4')
    	right -50%
    &:before
      position absolute
      bottom 0
      display block
      content ''
      width 100%
      height 100%
      z-index 100
      pointer-events none
      left -80%
      +mq('bp-4')
      	right -50%
        background linear-gradient(to left, rgba($bg,0) 0%,rgba($bg,1) 25%,rgba($bg,1) 100%)
  ul
    display flex
    flex-direction row
    height 8rem
    li
      display flex
      flex-direction column
      max-width 6rem
      height auto
      align-items: center
      margin 0 0.5rem

      .image
        //position absolute
        // top 0
        // left 50%
        margin 0 auto
        width auto
        max-height 3rem
        // background-position center
        // background-repeat no-repeat
        // background-size 5rem auto
        // transform translate(-50%, 0)
      .object-name
        text-align: center
        set-type-size('small', true)
